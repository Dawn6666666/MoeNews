# 本机开发环境信息与集成开发环境指南

本文档记录了当前主机的开发环境配置情况，并提供了在集成开发环境中开发与导入新闻发布与管理系统的指南。

## 1. 软件环境与安装路径

* 集成开发环境：IntelliJ IDEA 2025.2.4
  * 安装路径：D:\Jetbrains\IntelliJ IDEA 2025.2.4
  * 说明：无全局环境变量。

* JDK 环境：
  * 系统默认版本：OpenJDK 17.0.18 (已配置环境变量，可直接执行)
  * 本地安装路径：C:\Users\24831\.jdks
  * 检测到的可用版本列表：
    * corretto-1.8.0_492 (JDK 8)
    * ms-17.0.18 (JDK 17)
    * ms-21.0.10 (JDK 21)
    * oracleJdk-21 (JDK 21)
    * corretto-23.0.2 (JDK 23)
    * corretto-24.0.2 (JDK 24)
    * openjdk-25.0.1 (JDK 25)

* 前端开发环境：
  * Node.js版本：v22.21.1 (已配置环境变量，安装路径为 C:\nvm4w\nodejs\node.exe)
  * npm版本：10.9.4 (已配置环境变量)
  * Git版本：2.45.1.windows.1 (已配置环境变量，安装路径为 C:\Program Files\Git\cmd\git.exe)
  * 提示：如果在本机 PowerShell 终端执行 npm 命令时遇到禁止运行脚本的错误（关于 Execution_Policies 限制），可通过运行 `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser` 允许运行脚本，或者在执行命令时使用 `npm.cmd` 代替 `npm`。

* 数据库服务：MySQL 8.0
  * 服务名称：MySQL80
  * 服务状态：正在运行
  * 监听端口：3306
  * 安装路径：C:\Program Files\MySQL\MySQL Server 8.0
  * 说明：无全局环境变量。

* 项目构建工具：
  * Maven：本地命令行未配置全局 Maven 环境变量。
  * 本地已缓存的 Maven 包装器版本路径：
    * C:\Users\24831\.m2\wrapper\dists\apache-maven-3.9.15\0226a00282e400185496f3b60ec5a3f029cbdc6893912937d4876d57695224e1
  * 集成开发环境内置的 Maven 路径：
    * D:\Jetbrains\IntelliJ IDEA 2025.2.4\plugins\maven\lib\maven3

---

## 2. 当前项目结构

目前已为您搭建好项目整体骨架，具体结构如下：

* sql：存放数据库初始化脚本。
  * 脚本文件：d:\Code\IDEA\MoeNews\sql\init.sql (已编写核心数据表结构及默认用户和分类数据)。
* backend：后端 Spring Boot 3.x 项目工程。
  * 项目配置文件：d:\Code\IDEA\MoeNews\backend\pom.xml (已引入 Web、Validation、MyBatis-Plus、MySQL、Lombok、JWT 和加密等依赖)。
  * 主启动类：d:\Code\IDEA\MoeNews\backend\src\main\java\com\moenews\MoeNewsApplication.java。
  * 配置文件：d:\Code\IDEA\MoeNews\backend\src\main\resources\application.yml (已配置数据库连接、MyBatis-Plus 选项和本地上传路径)。
* frontend：前端 Vue 3 + Vite 项目工程。
  * 已完成脚手架的初始化，后续可直接在此目录中进行前端页面及接口联调开发。

---

## 3. 集成开发环境导入与开发指南

### 3.1 导入项目步骤

1. 打开 IntelliJ IDEA，选择打开项目 (Open)。
2. 选择 MoeNews 的根目录 `D:\Code\IDEA\MoeNews` 确认打开。
3. 打开后，IDEA 会自动检测并提示加载 Maven 模块，点击确认或等待 Maven 自动同步依赖即可。

### 3.2 数据库连接与建表

1. 本机 MySQL 服务（MySQL80）已在 3306 端口运行，请使用您的客户端工具（如 Navicat，或 IntelliJ IDEA 右侧的 Database 面板）连接到该服务。
2. 连接成功后，打开并运行位于 `d:\Code\IDEA\MoeNews\sql\init.sql` 的脚本，它将自动创建名为 `moenews` 的数据库以及相关的用户、分类、新闻和评论数据表，并导入初始的管理员、普通用户和新闻分类数据。

### 3.3 修改后端配置文件

1. 数据库密码默认为空。如果您本机的 MySQL 用户设置了密码，请在导入后打开 [application.yml](file:///d:/Code/IDEA/MoeNews/backend/src/main/resources/application.yml) 文件。
2. 找到 `spring.datasource.password` 并填写您的 MySQL 数据库连接密码。

### 3.4 后端启动与运行

1. 在 IntelliJ IDEA 中定位到 [MoeNewsApplication.java](file:///d:/Code/IDEA/MoeNews/backend/src/main/java/com/moenews/MoeNewsApplication.java) 文件。
2. 右键点击并选择运行，后端服务将启动并监听 8080 端口。

### 3.5 前端依赖安装与启动

1. 在集成开发环境的内置终端窗口中，将工作目录切换到 `frontend`。
2. 执行以下命令安装前端依赖：
   ```bash
   npm.cmd install
   ```
3. 安装完毕后，执行以下命令启动前端开发服务器：
   ```bash
   npm.cmd run dev
   ```
4. 启动完成后，可通过浏览器访问前端开发控制台（通常为 http://localhost:5173）。
